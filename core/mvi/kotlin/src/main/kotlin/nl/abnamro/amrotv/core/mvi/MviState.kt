package nl.abnamro.amrotv.core.mvi

/**
 * Marker interface for all MVI screen state types.
 *
 * Implementations must be immutable `data class` objects with sensible defaults for all fields.
 */
interface MviState
